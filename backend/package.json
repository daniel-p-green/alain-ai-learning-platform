{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "bun",
  "scripts": {
    "build": "sh -c 'if [ -d ../_legacy_frontend ]; then cd ../_legacy_frontend && bun install && vite build --outDir=../backend/frontend/dist; else echo \"legacy frontend not present\"; fi'",
    "test:pure": "vitest run export/notebook.test.ts execution/spec/lessonSchema.test.ts execution/providers/aliases.test.ts utils/hf.test.ts execution/teacher.test.ts execution/repair.test.ts --reporter=dot",
    "test:all": "vitest run --reporter=dot"
  },
  "dependencies": {
    "@clerk/backend": "^1.34.0",
    "encore.dev": "^1.49.3",
    "openai": "^4.104.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "vitest": {
    "setupFiles": [
      "./vitest.setup.ts"
    ]
  }
}
