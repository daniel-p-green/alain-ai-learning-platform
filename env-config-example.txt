# ALAIN AI Learning Platform - Environment Configuration
# Copy this file to .env.local and fill in your values

# === CLERK AUTHENTICATION (for web app) ===
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key_here
CLERK_SECRET_KEY=your_clerk_secret_key_here
NEXT_PUBLIC_TEACHER_ALLOW_120B=0

# === AI API CONFIGURATION ===
# Poe API Key (get from https://poe.com/api_key)
POE_API_KEY=***REMOVED***

# OpenAI Compatible API Configuration
# Note: For ALAIN‑Kit CLI (--baseUrl), pass the provider root WITHOUT /v1
#   e.g., --baseUrl https://api.poe.com  (CLI appends /v1/chat/completions)
# For raw OpenAI SDK usage in code, include /v1 as usual.
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=your_openai_api_key_here
TEACHER_PROVIDER=poe

# Teacher model policy (defaults)
# By default ALAIN uses GPT-OSS-20B as the teacher model.
# GPT-OSS-120B is disabled unless explicitly enabled below.
# Set to 1/true/yes/on to allow 120B; otherwise requests are downgraded to 20B.
TEACHER_ALLOW_120B=0

# === BACKEND SECRETS (for Encore.dev) ===
# These are configured through Encore.dev dashboard
# POE_API_KEY=***REMOVED***
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_API_KEY=your_openai_api_key_here
# TEACHER_PROVIDER=poe # or 'openai-compatible' for local (e.g., Ollama)
# Tip for local OpenAI-compatible servers: set OPENAI_BASE_URL to include /v1
#   Example: http://localhost:11434/v1 (Ollama), http://localhost:1234/v1 (LM Studio)

# === OPTIONAL CONFIGURATIONS ===
# Database URL (if using custom database)
# DATABASE_URL=your_database_url_here

# Development settings
NODE_ENV=development

# Prompt loading (optional overrides)
# TEACHER_PROMPT_PHASE=research            # or research.offline, design, develop, validate, orchestrator
# ALAIN_PROMPT_ROOT=/abs/path/to/resources/prompts/alain-kit  # override prompt templates (CLI + backend)
