# ALAIN Backend (Encore secrets are preferred over env files)

# Poe (hosted)
POE_API_KEY=qZB4vYDsDMytPrL9t4N4SGi_M56X_b0vst0l1fuJu9s

# OpenAI-compatible (local/offline)
OPENAI_BASE_URL=http://localhost:11434/v1
OPENAI_API_KEY=ollama

# Teacher provider default (poe | openai-compatible)
TEACHER_PROVIDER=poe

# Strict offline mode (skip external metadata fetches)
OFFLINE_MODE=0

# Local demo bypass for auth (enables unauthenticated cURL tests)
DEMO_ALLOW_UNAUTH=0

# Notes:
# - In production, set these via Encore secrets:
#   encore secret set POE_API_KEY
#   encore secret set OPENAI_BASE_URL
#   encore secret set OPENAI_API_KEY
#   encore secret set TEACHER_PROVIDER
#   encore secret set OFFLINE_MODE
#   encore secret set DEMO_ALLOW_UNAUTH
